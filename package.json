{
    "name": "docker-weather-bot",
    "description": "Dockerization of weather-bot project",
    "version": "0.0.1",
    "dependencies": {},
    "scripts": {
        "start": "docker-compose up --build -d",
        "stop": "docker-compose down",
        "update": "git submodule update --recursive --remote"
      }
}